from django.urls import path
from . import views

urlpatterns = [
    path('', views.profile, name='profile'),
    path('order_history/<order_number>',
         views.order_history, name='order_history'),
    path('repeat_order/<order_number>',
         views.repeat_order, name='repeat_order'),
]
